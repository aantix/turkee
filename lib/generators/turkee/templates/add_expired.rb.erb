require 'mechanical_turk_connection_migration'

class AddExpired< MechanicalTurkConnectionMigration

  def self.up
    self.with_connection do
      unless column_exists? :turkee_tasks, :expired
        add_column :turkee_tasks, :expired, :integer
      end
    end
  end

  def self.down
    self.with_connection do
      if column_exists? :turkee_tasks, :expired
  	  remove_column :turkee_tasks, :expired
    end
	end
  end
end
