require 'mechanical_turk_connection_migration'

class CreateTurkeeStudy < MechanicalTurkConnectionMigration

    def self.up
      self.with_connection do
        create_table "turkee_studies" do |t|
            t.integer "turkee_task_id"
            t.text "feedback"
            t.string "gold_response"
            t.timestamps
        end

        add_index :turkee_studies, :turkee_task_id
      end
    end

    def self.down
      self.with_connection do
        drop_table :turkee_studies
      end
    end

end
